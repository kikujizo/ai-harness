---
name: lateral-sweep
description: 不具合・指摘・事故が1件見つかったとき、それを失敗クラスに抽象化して全域を機械的に総点検し、正本まで還流するSkill。バグや指摘が1件見つかったとき、事故の再発防止を求められたとき、「同じようなミスがないか調べて」といった場面で発動する。1件の指摘を4クラスへ抽象化→grepで全域走査→修正/受容/バックログに三分する。
---

> このSkillの変更・修正は `.agents/skills/lateral-sweep/SKILL.md`（正本）を編集する。リンク先を編集しない。

# lateral-sweep: 1件を失敗クラスに広げて全域を掃く

指摘は1箇所で終わらせない。レビュアーが1箇所指摘したら、残りは自分で見つける。
assessment-first の横展開チェックから呼ばれることが多い。

## 手順

1. 指摘を**失敗クラス**に抽象化する（「何の欠如がこれを許したか」）。
   例: SSOT違反=正本宣言しつつ再掲 / 幻参照=実在しない手順への言及 /
   環境非対称=Windows製ゆえの欠陥 / 前提の暗黙化
2. クラスをgrep可能な特徴に落とし、対象全域を機械走査する。
   **大量走査は下位モデル・サブエージェントへ委譲**する（`docs/harness/ops/orchestration.md`）
3. 検出物を三分する: **修正** / **受容**（理由を記録。例: 外部ツールが読めない場所への複製は
   必須の負債）/ **バックログ**
4. 失敗クラスが正本起因なら上流へ還流する（`docs/harness/knowledge/reflux.md`）。
   Issueに全件と受容判断を記録し、修正PRを出す
5. 修正の適用先は「見つかった場所」だけでなく同クラスの全箇所

実例: レビュー指摘2件を4クラスに抽象化→正本37ファイルの機械点検で新規18件を検出→
修正PRに加えて受容・バックログをIssueに記録した。

## 制約

- 走査は**読み取り専用**で行う（掃きながら壊さない）
- 「修正しない」判断も必ず理由つきで記録する（無言のスキップ禁止）
- 記録はGitHubのIssue/PRへ（ルート`AGENTS.md`「GitHubドリヴン記録」）
- 日本語で出力（コード・識別子は原語）
