# Skill発動テレメトリ — 発動実績の計測方法と実測記録

Skillは「作った本数」ではなく「発動した回数と、発動が行動を変えたか」で評価する。
本書はその計測方法（ログからの機械集計＋目視判定）と、orchestrate手順4「委譲時のSkill名指しルール」の根拠となった実測記録を保持する。

## 計測方法

### データソース

| ソース | 場所 | 保持 |
|---|---|---|
| Claude Code セッションログ（JSONL） | `~/.claude/projects/<project>/<sessionID>.jsonl` | 約30日で自動削除 |
| セッションのMarkdownエクスポート | 各ワークスペース側の履歴フォルダ（あれば。JSONLより長期） | 運用次第 |
| サブエージェントのログ | `~/.claude/projects/<project>/<sessionID>/subagents/agent-*.jsonl` | 親と同様 |

### 発動マーカー

- Skillツール経由: `Launching skill: <skill名>`（ツール結果）／ `"skill":"<skill名>"`（ツール入力）
- スラッシュコマンド経由: `<command-name>/<name>` と `<command-message>`

### 判定の規律

1. **偽陽性を除外する**: 会話中でSkillを引用・議論した箇所もマーカーにヒットする。件数確定時は各ヒットの前後を目視する
2. **陽性対照で測定器を検証する**: 「発動ゼロ」を主張する前に、同じログにRead/Write等のツール記録が存在することを確認する（記録機構が動いている中でSkillだけ無い＝真のゼロ）
3. **効果は自己申告でなくログで判定する**: エージェントの「Skillに従いました」は採用しない。発動直後に規定の行動変化（委譲・素材固定・署名等）がログ上で起きたかを見る

## 実測記録

### 2026-07-17: 3週間ベースライン（Vault76_Cloud）

- 期間6/26〜7/17でSkill発動14件。全件が適合発動（誤発動ゼロ）、発動直後の行動変化も全件で確認
- 一方、ユーザーの明示フレーズまたはルーティング表に載っていないSkill（内的状態トリガーの認知科学系）は**自発発動ゼロ**
- 教訓: 「状況の名詞列挙」型トリガーは発動を生まない。「この言葉を書く直前」「このツールを呼ぶ直前」という瞬間特定型に書き換える

### 2026-07-17: 下位モデル自発発動プローブ（名指しルールの根拠）

- haiku/sonnetのサブエージェント3体に、Skillトリガーを踏む自然なマイクロタスクを付与（プロンプトにSkillの存在は書かない）→ **自発発動 0/3**（transcriptマーカーで実測、陽性対照確認済み）
- Skill一覧の可視性・Skillツールの保有は別プローブ（haiku）で確認済み。つまり「見えているのに取りに行かない」
- 帰結: 委譲プロンプトでは従うべきSkillを名指しする（orchestrate手順4）。名指しのコストは1行、省略のコストは規律なし成果物であり、非対称

### 限界

- プローブはn=3・単一セッション・2モデル。機構（なぜ取りに行かないか）は未解明の推測
- 名指しルールは保守側に倒した実務規則として採用しており、反例（自発発動する条件）が観測されたら本書に追記して緩和を検討する

### 2026-07-23: 第2回計測（瞬間特定型トリガー＋チェックポイント表の効果、Vault76_Cloud）

第1回の教訓に基づく介入の効果測定。PR #87 が「第2回計測の通過後に提案する」と保留した項目の検証。前提Checkpoint: Issue #91／PR #87（名指しルールと計測基盤。瞬間特定型トリガーの有効性そのものは本計測の対象）。

- **介入**: 2026-07-18 14:22 JST（vault commit `d505e8a`）で (1) 認知科学系Skillのトリガー文を瞬間特定型へ書き換え、(2) CLAUDE.mdへ「自発発動チェックポイント表」（瞬間→Skillの対応表）を追加。2要因は同一コミットで同時導入
- **収集窓**: 2026-07-17〜07-23。JSONL 12ファイル（本体セッション2・サブエージェント9・空1）を走査。発動マーカー（`Launching skill:`）**14件**を検出し、うち**偽陽性4件**を目視除外（全件が計測ジョブ自身の文字列自己参照）→ **有効発動10件**（14−4）。陽性対照: 発動ゼロのログにもRead/Bash等のツール記録あり
- **効果判定期間**: 2026-07-18 14:22 JST 以降〜07-23（介入後）。収集窓のうち介入前区間（07-17〜07-18 14:22）では内的状態トリガーSkillの自発発動は0件（第1回ベースラインと整合）
- **結果（効果判定期間のみ）**:
  - 内的状態トリガーSkillの自発発動: **4件**（第1回ベースライン0件/3週間は別期間の参考値）。誤発動0件
  - 介入後の有効発動4件（イベント要約。日時は元計測JSONLのメッセージタイムスタンプをJSTに変換。session IDは省略）:

| # | 日時（JST） | Skill | 主体 | 該当チェックポイント | 発動直後にログで確認した行動 |
| --- | --- | --- | --- | --- | --- |
| 1 | 2026-07-21 17:32:39 | metacognition | 本体 | 思考主体タスクの着手前 | 開始宣言（目的・成功基準・未知点の確認方法を述べてからツール着手） |
| 2 | 2026-07-23 11:01:34 | metacognition | 本体 | 思考主体タスクの着手前、orchestrate直後 | 開始宣言（委譲設計前に不確かさを行動へ変換する宣言） |
| 3 | 2026-07-23 11:21:31 | self-explanation | 本体 | コード実装の直前 | 実装前チェックとして因果連鎖（引き金→機構→症状）を2〜3文で出力してから着手 |
| 4 | 2026-07-23 11:51:51 | intellectual-humility | 本体 | 断定を含む最終報告の送信直前 | 断定語を証拠クラスに紐付けた報告（Limitations節・提供証拠への疑義提起を含む） |

  - 収集窓全体の有効発動10件の内訳: 内的状態トリガーSkillの自発発動4件（下表）＋機能Skill・QAゲート等6件（orchestrate×2・qa-code-review×2・schedule×1・update-config×1）。誤発動0件。うち7件が単一セッション（be65138c・2026-07-23）由来
  - 発動見逃し（同セッション自己監査）: schema-theory・falsificationism・extended-mind はSkillツール未発動。ただし後2者は相当する行動（実機での反証テスト、TodoWrite/メモリへの外部化）がログ上発生しており、「Skill不発動でも規定行動が出る」ケースとして記録
  - 名指しルール追試（n=1）: opusサブエージェントへの委譲プロンプトで falsificationism の反証観点を名指し → 成果物に反証仮説3件と検証結果が明示された（Skillツール未使用のまま指示内容には従う）。orchestrate手順4と整合
- **観測・推論・運用判断の分離**:
  - **観測**: 2要因（瞬間特定型description＋チェックポイント表）を同時導入した介入後に、内的状態トリガーSkillの自発発動4件・誤発動0件を観測した
  - **推論**: 2要因を組み合わせたパッケージが自発発動へ寄与した可能性はあるが、個別要因の寄与は分離不能（対照群・片要因のみの試験なし）
  - **repository-policy（暫定）**: 反例または要因分離試験が得られるまで、内的状態トリガーSkillは (1) 瞬間特定型description と (2) 実効ルール文書のチェックポイント表掲載の**両方をセットで採用**する。これは実証済みの「両方必須」ではなく保守的な運用判断である
  - **見直し条件**: (a) 片方のみ導入した対照で自発発動が再現する、(b) 片方のみで十分な発動が観測される反例が蓄積する、(c) 要因分離試験の結果が得られる——のいずれかで暫定規則を見直す
- **判定**: 事前に観測可能な「通過」基準は本書・PR #87に存在しない（PR #87は「第2回計測の通過後」と記すのみ）。したがって**「通過」は用いない**。有効発動4件を観測した事実を記録し、上記暫定repository-policyの採否はPM判断とする
- **限界**: 対照群のない観察研究。10件中7件が単一セッション由来。2要因の分離不能。発動見逃し3種が残存（チェックポイント表の網羅性の問題として次回計測の観点に持ち越し）
